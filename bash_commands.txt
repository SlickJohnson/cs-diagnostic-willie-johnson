Change directory == "cd {directory}"
List items in current directory == "ls"
Print working directory == "dirs"
Create a new file called “cookies.txt” == ??
Create a new folder called “recipes” == ""
Delete a file == ??
Stage a file in Git == "git add {filename}.{extension}" example: "git add bash_commands.txt"
Commit a file in Git == 'git commit -m "Describe commit here"'
Push the file to the Git remote’s origin, on the master branch. == "git push" 
